"use strict";
function greeter(person) {
    /**
     * @person {string} Name of user
     *
     */
    return "Hello, " + person;
}
var user = "ravin";
// document.body.innerHTML = greeter(user);
console.log(greeter(user));
